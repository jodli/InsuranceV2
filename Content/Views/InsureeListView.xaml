<UserControl x:Class="Content.Views.InsureeListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
            <DataGrid AutoGenerateColumns="True" ItemsSource="{Binding Path=InsureeData}" />
            <Button Content="Update List" Command="{Binding UpdateListCommand}"></Button>
        </StackPanel>
    </Grid>
</UserControl>